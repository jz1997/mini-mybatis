<mapper namespace="com.exmaple.small.mybatis.test.mapper.UserMapper">
    <select id="findById" parameterType="java.lang.Long"
            resultType="com.exmaple.small.mybatis.test.entity.User">
        select *
        from user
        where id = #{id}
    </select>

    <select id="findAll" parameterType="com.exmaple.small.mybatis.test.entity.User"
            resultType="com.exmaple.small.mybatis.test.entity.User">
        select *
        from user
    </select>

    <select id="findOne" parameterType="com.exmaple.small.mybatis.test.entity.User"
            resultType="com.exmaple.small.mybatis.test.entity.User">
        select *
        from user
        where id = #{id}
    </select>

    <select id="findByUsername" parameterType="java.lang.String"
            resultType="com.exmaple.small.mybatis.test.entity.User">
        select *
        from user
        where username = #{username}
    </select>

    <insert id="insert" parameterType="com.exmaple.small.mybatis.test.entity.User">
        insert into user(id, username, nickname, password)
        values (#{id}, #{username}, #{nickname}, #{password})
    </insert>

    <delete id="delete" parameterType="java.lang.String">
        delete
        from user
        where id = #{id}
    </delete>
</mapper>